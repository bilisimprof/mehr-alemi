# mehr alemi — V9

V9 archive presentation update:
- Desktop uses three core compositions: hero, split, portrait.
- Every sixth entry resets the rhythm with a wide hero scene.
- Mobile uses one consistent full-width scene flow: image → title → excerpt → read link.
- Reveal animations are softened and staggered with transform/opacity only.
- Desktop parallax is intentionally subtle; it is disabled on mobile and reduced-motion settings.
- The archive counter reports the full filtered collection (e.g. 01 / 30), not only the currently loaded six entries.
- The "daha fazlasını gör" control still reveals six entries at a time.
